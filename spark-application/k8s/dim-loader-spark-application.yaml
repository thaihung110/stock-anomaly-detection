apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata:
  name: dim-loader
  namespace: stock-anomaly-detection
spec:
  type: Scala
  mode: cluster
  image: "hungvt0110/dim-loader:v0.7"
  imagePullPolicy: Always
  timeToLiveSeconds: 3600
  mainClass: com.stockanomalydetection.dimloader.DimLoaderApp
  mainApplicationFile: "local:///opt/spark/jars/dim-loader-assembly-1.0.0.jar"
  sparkVersion: "3.5.1"

  sparkConf:
    # -------------------------------------------------------------------------
    # 1) SQL extensions and catalog routing
    # -------------------------------------------------------------------------
    "spark.sql.extensions": "org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions"
    "spark.sql.catalog.spark_catalog": "org.apache.iceberg.spark.SparkSessionCatalog"
    "spark.sql.catalog.spark_catalog.type": "in-memory"
    "spark.sql.defaultCatalog": "gravitino_gold"

    # -------------------------------------------------------------------------
    # 2) Spark execution tuning — small data, all dims < 15k rows
    # -------------------------------------------------------------------------
    "spark.sql.adaptive.enabled": "true"
    "spark.sql.adaptive.coalescePartitions.enabled": "true"
    "spark.sql.adaptive.advisoryPartitionSizeInBytes": "134217728"
    "spark.sql.shuffle.partitions": "2"
    "spark.default.parallelism": "2"

    # -------------------------------------------------------------------------
    # 3) Iceberg write behaviour
    # -------------------------------------------------------------------------
    "spark.sql.iceberg.distribution-mode": "hash"
    "spark.sql.iceberg.advisory-partition-size": "134217728"

    # -------------------------------------------------------------------------
    # 4) S3A / MinIO filesystem
    # -------------------------------------------------------------------------
    "spark.hadoop.fs.s3a.path.style.access": "true"
    "spark.hadoop.fs.s3a.impl": "org.apache.hadoop.fs.s3a.S3AFileSystem"
    "spark.hadoop.fs.s3a.aws.credentials.provider": "org.apache.hadoop.fs.s3a.SimpleAWSCredentialsProvider"

    # -------------------------------------------------------------------------
    # 5) Data quality compatibility
    # -------------------------------------------------------------------------
    "spark.sql.iceberg.check-nullability": "false"

  restartPolicy:
    type: Never

  driver:
    cores: 1
    memory: "512m"
    memoryOverhead: "256m"
    serviceAccount: spark
    env:
      - name: GRAVITINO_URI
        value: "http://openhouse-gravitino:9001"
      - name: MINIO_ENDPOINT
        value: "http://openhouse-minio:9000"
      - name: GRAVITINO_OAUTH_SERVER_URI
        value: "http://openhouse-keycloak"
      - name: GRAVITINO_OAUTH_TOKEN_PATH
        value: "realms/iceberg/protocol/openid-connect/token"
      - name: GRAVITINO_OAUTH_SCOPE
        value: "gravitino"
      - name: INPUT_TABLE
        value: "gravitino_bronze.raw.raw_company_info"
      - name: OUTPUT_NAMESPACE
        value: "gravitino_gold.gold"
      - name: MINIO_ACCESS_KEY
        valueFrom:
          secretKeyRef:
            name: spark-app-secrets
            key: MINIO_ACCESS_KEY
      - name: MINIO_SECRET_KEY
        valueFrom:
          secretKeyRef:
            name: spark-app-secrets
            key: MINIO_SECRET_KEY
      - name: AWS_ACCESS_KEY_ID
        valueFrom:
          secretKeyRef:
            name: spark-app-secrets
            key: MINIO_SECRET_KEY
      - name: AWS_SECRET_ACCESS_KEY
        valueFrom:
          secretKeyRef:
            name: spark-app-secrets
            key: MINIO_SECRET_KEY
      - name: GRAVITINO_OAUTH_CLIENT_SECRET
        valueFrom:
          secretKeyRef:
            name: spark-app-secrets
            key: GRAVITINO_OAUTH_CLIENT_SECRET
    labels:
      app.kubernetes.io/component: driver
      app.kubernetes.io/name: dim-loader

  executor:
    instances: 1
    cores: 1
    memory: "512m"
    memoryOverhead: "256m"
    env:
      - name: GRAVITINO_URI
        value: "http://openhouse-gravitino:9001"
      - name: MINIO_ENDPOINT
        value: "http://openhouse-minio:9000"
      - name: GRAVITINO_OAUTH_SERVER_URI
        value: "http://openhouse-keycloak"
      - name: GRAVITINO_OAUTH_TOKEN_PATH
        value: "realms/iceberg/protocol/openid-connect/token"
      - name: GRAVITINO_OAUTH_SCOPE
        value: "gravitino"
      - name: INPUT_TABLE
        value: "gravitino_bronze.raw.raw_company_info"
      - name: OUTPUT_NAMESPACE
        value: "gravitino_gold.gold"
      - name: MINIO_ACCESS_KEY
        valueFrom:
          secretKeyRef:
            name: spark-app-secrets
            key: MINIO_ACCESS_KEY
      - name: MINIO_SECRET_KEY
        valueFrom:
          secretKeyRef:
            name: spark-app-secrets
            key: MINIO_SECRET_KEY
      - name: AWS_ACCESS_KEY_ID
        valueFrom:
          secretKeyRef:
            name: spark-app-secrets
            key: MINIO_SECRET_KEY
      - name: AWS_SECRET_ACCESS_KEY
        valueFrom:
          secretKeyRef:
            name: spark-app-secrets
            key: MINIO_SECRET_KEY
      - name: GRAVITINO_OAUTH_CLIENT_SECRET
        valueFrom:
          secretKeyRef:
            name: spark-app-secrets
            key: GRAVITINO_OAUTH_CLIENT_SECRET
    labels:
      app.kubernetes.io/component: executor
      app.kubernetes.io/name: dim-loader
